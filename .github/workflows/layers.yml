# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: layers
on:
  pull_request:
    paths:
      - extension-go/**
      - scripts/build-layers.ts
      - scripts/zip.ts
      - assets/**
      - .go-version
      - .github/workflows/layers.yml
  push:
    branches:
      - main
    paths:
      - extension-go/**
      - scripts/build-layers.ts
      - scripts/zip.ts
      - .go-version
      - .github/workflows/layers.yml
  workflow_dispatch: {}
jobs:
  build-layers:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - id: auto_60120a02
        uses: actions/checkout@v7
      - id: auto_a7dbad2d
        uses: actions/setup-go@v7
        with:
          go-version-file: .go-version
          cache: true
          cache-dependency-path: extension-go/go.sum
      - id: auto_60fc0a66
        uses: actions/setup-node@v7
        with:
          node-version: 24.x
      - name: go vet + test
        id: go_vet_test
        run: cd extension-go && go vet ./... && go test -race ./...
      - name: install
        id: install
        run: npm ci
      - name: rebuild layers
        id: rebuild_layers
        run: npx projen build:layers
      - name: fail if assets are stale
        id: fail_if_assets_are_stale
        run: 'git diff --exit-code --stat -- assets/ || (echo "::error::assets/ is stale - run: npx projen build:layers" && exit 1)'
